{
  "name": "quotes",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "turbo run build --filter=./apps/client",
    "start:dev": "turbo run start:dev --filter=./apps/client",
    "code:check": "turbo run code:check --filter=./apps/client",
    "code:lint": "turbo run code:lint",
    "code:lint:autofix": "turbo run code:lint:autofix"
  },
  "devDependencies": {
    "turbo": "^2.5.5"
  },
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*"
  ]
}
